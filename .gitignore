# Protected clinical data — MIMIC-IV requires credentialed PhysioNet access; never commit
output/data/

# OS / editor junk
.DS_Store
*.swp
*~
*.tmp

# Local helper scripts used during repo assembly (not part of the release)
patch_docx.py

# v34 nested clinical-data dir (MIMIC-IV derived; never commit)
manuscript_v34/output/data/

# v37 large simulation output (~19 MB; regenerate via R/16_sim_v37p1_80grid.R)
simulation/comparison_results.rds
simulation/comparison_agg.rds

# v38: large simulation objects (regenerate via R/16_sim_v37p1_80grid.R + R/02d_merge.R)
output/simulation/
output/data/
# macOS 编辑器副本与备份
*copy*.docx
*.bak*
_prerender_backup/

# v39: large simulation objects (tens of MB; regenerate via R/16 and R/27-R/29)
simulation/v39_ba_gibbs_reps.rds
simulation/v39_interval_joint_reps.rds

# 同步/渲染过程中产生的备份目录
*_backup*/
_prerender_backup_*/
